// Create a new variable called wallet as an integer
// Create a boolean called spend
// Create a string called notifySpend
// Initialize the wallet to 500
//set the value of spend to False
// add the strin literal (the strin value) 'you have spent money' to notifySpend 

// SPDX-License-Identifier: MIT
pragma solidity >= 0.7.0 < 0.9.0;

contract exercise1 {
    uint wallet = 500;
    bool spend = false;
    string notifySpend = 'You have spend money';
    
}